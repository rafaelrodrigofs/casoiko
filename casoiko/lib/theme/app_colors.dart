import 'package:flutter/material.dart';

/// Paleta Casoiko — soft blue (alinhada ao FigmaShow / ref. Pinterest).
abstract final class AppColors {
  static const background = Color(0xFFEEF5FC);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceMuted = Color(0xFFF0F2F5);

  static const primary = Color(0xFF3B82F6);
  static const primaryDark = Color(0xFF2563EB);
  static const primaryMid = Color(0xFF60A5FA);
  static const primarySoft = Color(0xFFDBEAFE);

  static const textPrimary = Color(0xFF1E293B);
  static const textSecondary = Color(0xFF64748B);

  static const border = Color(0xFFE5E7EB);
  static const success = Color(0xFF16A34A);
  static const danger = Color(0xFFDC2626);
  static const warning = Color(0xFFD97706);
}
