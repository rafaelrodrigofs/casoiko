# Assets locais do FigmaShow

Coloque imagens aqui e referencie nos nós `image` com `src: "/assets/nome.png"`.

Em produção (Docker/Coolify), o servidor Express serve este diretório em `/assets`
a partir de `$FIGMASHOW_DATA/assets` (volume `/data/assets`).
